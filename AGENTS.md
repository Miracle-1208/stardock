# AGENTS.md

本文件适用于整个仓库。除非更深层目录存在范围更窄的 `AGENTS.md`，所有在本项目中工作的开发者和编码 Agent 都必须遵守这里的约定。

## 最高优先级：八条不可协商的工程规则

以下八条是本项目的最高级工程约束，不是建议，也不能以“兼容性”“以后可能需要”“先搭框架”或“测试方便”为理由绕过、稀释或改写。任何实现方案、重构、测试和文档都必须首先满足它们；违反其中任意一条，任务即视为未完成。

1. 不保留向后兼容。过时的直接删，别加兼容层、别写 migration、别留 fallback。
2. 选能满足当前需求的最简单实现。不要预防性抽象，不要多此一举的配置层。
3. 系统分层长。先跑通一个最小的端到端版本，再往上加东西。绝不为了未完成的复杂度拆掉能跑的东西。
4. 组件保持模块化，关注点分离。
5. 优先用成熟的、有人维护的库。没有明确理由别自己重写。
6. 先翻项目里已有的依赖能做什么，再考虑加新包或自己写。别上来就假设库里没有。
7. 架构决策往长了做。不接受“先这样以后再换”的临时方案。
8. 先看成熟产品怎么解决同一个问题，用已验证的模式，别从零发明。

执行这些规则时，还必须遵守以下含义：

- 替换旧能力时，删除旧入口、旧配置、旧实现和只服务于旧实现的测试；不要让新旧路径并存。
- 先证明一条真实、最短的端到端链路可运行，再扩充能力、覆盖面和优化项。
- 模块化不等于增加层级。只有职责、生命周期或依赖边界确实不同，才拆分组件。
- 新增依赖前先检查 `requirements.txt`、`package.json`、现有模块和标准库；新增依赖必须能显著降低维护成本。
- 关键架构选择必须适合长期维护，但当前实现只覆盖已确认的需求，不预埋假设性的扩展点。

## 高优先级：禁止防御递归与验收漂移

安全和可靠性措施必须服务于当前明确的威胁模型、真实故障和验收合同。修复一个门禁后，不得继续递归假设“保护措施本身再失败怎么办”，并据此不断增加 marker、恢复层、权限层、扫描规则、fallback 或新状态机。没有现实证据、稳定复现或明确合同依据的防御性设想，不得作为 high/critical 阻塞项。

- 已明确按受信任主机、受信任开发者或受信任候选代码运行的任务，不得擅自扩大为敌对代码执行平台，也不得追加沙箱、凭据代理、权限 profile 或通用隔离框架。
- 审查 finding 必须指向当前范围内可复现的错误、直接合同违例、数据破坏、安全边界突破或用户可观察失败；本地日志写入、诊断清理、极端文件系统故障等假设，除非已发生或合同明确要求，否则不得升级为阻塞性交付风险。
- 达到约定修正次数后必须停止修改并交由上级任务处理，不能用连续“再修一点”把短任务拖成长上下文重构。
- 当防御机制开始制造比原问题更多的状态和失败面时，优先删除或简化该机制，保留一个清晰的失败结果，而不是继续为它增加恢复机制。
- 独立审查必须服从任务既定范围和威胁模型。不得因为能够想象出更极端的失败，就改写已接受的架构决定或完成标准。

## 高优先级：禁止补丁式开发

“别写 migration”的核心含义是：**禁止为了保留旧结构、支持回滚、兼容历史数据或绕开彻底修改，而不断追加补丁式 migration。它不代表禁止合理的数据库 schema 变更。** 本项目仍处于开发阶段，数据库结构必须直接反映当前目标架构。

- 功能确实需要新表、字段、索引或约束时，允许并且应当修改 schema；不要为了避免 schema 变更而把数据塞进不相关 JSON、文件缓存或运行时临时结构。
- 开发阶段优先重写、合并或重新建立干净的基线 migration，使全新数据库一次初始化后就是目标结构；不要为每次修改叠加 `add_*`、`alter_*`、`backfill_*`、`compat_*` 小补丁。
- 结构变化后直接重建开发数据库和派生索引，不维护旧数据库的升级链、降级链、双写、旧列读取或历史数据兼容逻辑。
- 禁止在应用启动时执行 `CREATE TABLE IF NOT EXISTS`、条件式 `ALTER TABLE` 或其他隐式 schema 修补；schema 只能由清晰、可审查的数据库定义创建。
- 禁止用一次性数据修复脚本掩盖错误的数据模型。可重新生成的数据应从权威来源重建；必须保留的数据应按当前模型明确导入，而不是长期保留转换层。
- 同一概念只能有一个当前 schema、一个读写路径和一个数据合同。修改完成后，旧表、旧列、旧 Repository 方法及对应测试一并删除。
- 提交前必须从空数据库验证完整初始化和最小端到端链路，不能只在经历过多轮手工修补的本地数据库上验证。

## 高优先级：严禁堆砌无效测试

测试代码同样是需要长期维护的生产资产，不是数量指标。**不要为了显得覆盖充分、让 diff 更大、让 CI 更好看或“顺手补一下”而写测试。** 没有保护真实风险的测试就是负债，必须不写或删除。大量低价值测试不代表质量高；它们会锁死实现、掩盖关键失败并拖慢重构。

新增任何测试前，必须能明确回答：它保护了哪个用户可观察行为、业务约束、数据契约、安全边界或曾经发生的回归？如果答案只是“这个函数存在”“这行代码被调用”“提高覆盖率”或“以后可能有用”，就不要写。

严禁新增以下测试：

- 只断言 mock 被调用几次、调用顺序或内部参数拼装，却不验证最终业务结果。
- 为私有函数、简单 getter/setter、薄包装、常量、字段转发或标准库行为逐个补测试。
- 把实现代码在测试里重写一遍，再证明两份相同逻辑得到相同结果。
- 用全套 fake client、fake model、fake MCP、fake repository 拼出一个假的“端到端测试”。
- 对提示词全文、日志文案、内部类名、临时数据结构或无契约保证的顺序做脆弱断言。
- 为每个相似输入复制一份测试，而不是保留少量有边界意义的参数化案例。
- 只为追求行覆盖率而触发没有行为意义的分支、异常或防御性代码。
- 为已经删除或即将替换的旧 API、旧配置、兼容路径和 fallback 保留测试。
- 已有更高层测试完整覆盖同一风险时，再增加重复的低层测试。
- 测试永远不会因真实实现错误而失败，或只证明 mock 按预设返回值工作。

允许新增的测试应尽量少，并优先覆盖：

- 会影响用户结果或持久化数据的核心业务规则。
- API、数据库、MCP 工具和模型结构化输出之间的稳定契约。
- 幂等、事务、状态转换、发布门槛、引用链和安全校验等高风险边界。
- 已确认缺陷的最小回归案例。
- 无法通过更高层真实验证稳定覆盖的关键确定性逻辑。

修改实现时，不要机械地为每个新增函数配一个测试文件。先检查现有测试是否已经保护目标行为；能扩充一个现有高价值案例，就不要另起重复套件。重构后行为不变时，测试应尽量保持不变；如果测试因内部实现变化大面积失败，优先判断这些测试是否绑错了层级并将其删除或改成行为断言。

评审测试时采用删除优先原则：删除该测试后，如果一个可信的业务回归、安全问题或契约破坏仍会被其他测试发现，那么这个测试通常是重复的；如果它本身也无法发现这类问题，那么它就是无效的。不得以测试数量、覆盖率百分比或快照数量作为任务完成标准。

## 项目介绍

### StarDock 是什么

StarDock（星坞）是一个面向大学生的 AI Course Workspace 高保真网页原型，核心主张是：

> 一门课，一个长期存在的 AI 工作区。

它把课件、教材、笔记、作业、录音、往年试卷和课程通知组织到课程上下文中，让 AI 能结合课程资料、知识结构与学生的学习行为给出下一步行动。产品核心循环是：

```text
课程资料 → 课程理解 → 学习活动 → 学生状态 → AI 行动
```

当前页面用于产品设计展示和概念验证，不承诺真实业务能力，也不要求所有按钮都可用。除非任务明确要求，不要把展示型入口擅自扩展成真实后端功能。

### 当前产品范围

当前原型主要展示：

- 学期首页与多课程入口。
- 概率论课程主页、课程地图和今日学习计划。
- 带课程上下文的 AI 对话、资料引用和思考过程。
- 课程资料库、模拟上传和跨资料搜索。
- 课程笔记、自适应练习、错题反馈与掌握度更新。
- 考试复习重点、模拟试卷入口和学期日历。

现阶段默认优先优化桌面网页端。除非任务明确提出移动端、窄屏或响应式要求，不要主动扩大到移动端重新设计。

### 当前技术状态

- 所有课程数据、AI 回答、上传处理和学习状态均为前端 Mock。
- 当前没有真实登录、持久化数据库、文件上传服务或 LLM API。
- `db/schema.ts` 目前是空 schema 占位；不要因为目录存在就假定数据库已经投入使用。
- `.openai/hosting.json` 表明项目使用 Sites 托管能力，但当前没有声明 D1 或 R2 资源。
- 页面采用单路由应用式原型，通过本地 React 状态切换首页、课程、资料和日历等视图。

## 技术栈与目录

### 技术栈

- React 19
- TypeScript
- vinext / Vite
- Lucide React
- 原生 CSS、CSS 动画与响应式布局
- Cloudflare Worker 兼容的构建输出

### 关键目录和文件

- `app/page.tsx`：主要产品页面、Mock 数据和原型交互。
- `app/globals.css`：全局视觉系统、组件样式、动画和布局断点。
- `app/layout.tsx`：页面元数据、字体和根布局。
- `app/chatgpt-auth.ts`：ChatGPT/Sites 运行环境相关入口。
- `worker/index.ts`：vinext 的 Cloudflare Worker 入口与图片优化处理。
- `db/schema.ts`：数据库 schema 占位；当前没有业务表。
- `db/index.ts`：数据库接入占位。
- `tests/rendered-html.test.mjs`：构建产物的最小 HTML 行为检查。
- `.openai/hosting.json`：Sites 项目与托管资源声明。
- `public/`：图标和 Open Graph 预览图等静态资源。

## 本地开发与验证

需要 Node.js 22.13 或更高版本。

```bash
npm install
npm run dev
```

常用验证命令：

```bash
npm run build
npm run lint
npm test
```

按改动风险选择最小而充分的验证方式：

- 页面结构、样式或交互修改至少运行 `npm run build`。
- 只有 lint 规则相关或任务明确要求时才额外运行 `npm run lint`。
- 只有现有高价值测试覆盖了本次风险，或新增了真实行为合同，才运行或修改对应测试。
- 视觉验收应检查任务明确涉及的桌面尺寸和页面状态；不要把未要求的移动端检查扩张成交付范围。

## 最高优先级：每次更改后的交付动作

任何代码、样式、配置、文档或资源更改，在完成与风险匹配的验证后，都必须继续完成以下交付动作。只完成本地修改、只构建成功或只给出预览不算任务完成。

本项目在当前账户下的固定生产站点是：

> https://stardock-learning-space.bdxtvbhvgy.chatgpt.site

该链接是后续所有部署的唯一目标。对应 Sites 项目以 `.openai/hosting.json` 中的 `project_id` 为准。

1. 将本次确认完成的更改提交到当前 Git 分支，提交内容必须与已经验证的源码一致。
2. 将提交推送到仓库现有远端；除非任务明确要求，不新建远端、替换仓库或改写分支历史。
3. 使用 `.openai/hosting.json` 中现有的 `project_id` 重新部署到 ChatGPT Sites。
4. 必须更新现有站点版本并保持 `https://stardock-learning-space.bdxtvbhvgy.chatgpt.site` 不变，不得为同一项目另建站点、生成替代链接或只发布临时预览。
5. 部署成功后确认该同一链接已经指向本次提交对应的版本，并在最终回复中报告提交、推送和部署结果。

执行这套交付动作时还必须遵守：

- 提交前检查工作区，只纳入当前任务确认范围内的文件；不要夹带无关改动。
- 推送或部署失败时，报告具体阻塞并保留已经完成的本地工作；不得静默跳过，也不得声称任务已经完整交付。
- 不得通过创建新的 Sites 项目绕过现有项目的权限、凭据或部署问题。
- 文档修改也属于更改，必须遵守相同的提交、推送和同链接重新部署流程。
- 除非用户明确要求仅在本地修改，否则不得省略上述任一动作。

## 项目开发原则

### 先守住产品叙事

- 每次修改都应强化“长期课程上下文驱动学习行动”这一核心，而不是把产品做成普通网盘、通用聊天框或校园功能集合。
- 首页、课程、资料、笔记、练习、考试和日历应共享一致的课程、时间和学习状态叙事。
- 使用具体、可信的课程内容和学习场景；避免空洞的占位文案、模板化仪表盘指标和与大学生学习无关的通用 SaaS 元素。
- 这是高保真展示原型。展示完整的信息结构和关键状态即可，不要为了让每个入口都可点击而引入无关状态或伪后端。

### 保持现有视觉语言

- 延续当前克制、编辑感强的桌面视觉：米白画布、细分割线、低饱和课程色、充足留白和局部衬线标题。
- 优先复用 `app/globals.css` 中的颜色变量、字体、间距、按钮和标题模式。
- 同一课程在不同页面始终使用一致颜色；颜色用于表达课程、状态和重点，不用于无意义装饰。
- 信息密度应服务于快速理解。重要行动、截止日期和学习反馈优先于装饰性统计。
- 不要随意引入新的设计系统、CSS-in-JS、组件库或图标库；先使用现有 CSS 和 Lucide React。

### 控制实现范围

- 修改前先阅读相关组件、样式、现有依赖和相邻页面，确认能复用什么。
- 小型页面优化优先在现有 `app/page.tsx` 和 `app/globals.css` 中完成；只有出现明确独立职责、生命周期或复用边界时才拆文件。
- 不因“以后可能接后端”提前增加 service、repository、adapter、provider、feature flag 或配置层。
- 不因项目存在 Drizzle、D1 或 Worker 相关依赖就默认启用数据库、鉴权、存储或 API。
- 如果任务只是优化展示，不要顺手实现真实上传、登录、AI 调用、日历同步或数据持久化。
- 不添加与当前任务无关的响应式规则、兼容逻辑、容错层或抽象。

### 修改和清理要求

- 替换页面或组件时，删除旧 JSX、旧样式、旧状态和仅服务于旧实现的代码。
- 不允许把新实现追加在旧实现旁边，再靠条件分支或隐藏样式维持两套路径。
- 不修改与任务无关的文件，不覆盖工作区中已有的用户改动。
- 新依赖必须有明确、当前且不可由现有依赖简单满足的理由；否则不添加。
- 代码中的 Mock 数据应靠近其使用位置并保持易读；不要为静态展示数据建立多层配置系统。

## 完成标准

任务完成前确认：

1. 实现直接满足当前明确需求，没有附带未要求的功能。
2. 旧入口、旧实现和废弃样式已删除，没有新旧路径并存。
3. 产品文案、Mock 数据、课程颜色和页面状态保持一致。
4. 组件职责清楚，但没有为了“模块化”增加无意义层级。
5. 没有新增兼容层、fallback、防御性状态机或假设性扩展点。
6. 没有新增低价值测试；现有测试未绑定到不稳定的内部实现。
7. 已完成与改动风险匹配的构建或行为验证。
8. 已提交并推送到当前远端分支。
9. 已使用现有 Sites 项目重新部署，同一个 `chatgpt.site` 链接已更新。
10. 最终说明只报告实际完成内容、验证结果和仍然存在的明确限制。
